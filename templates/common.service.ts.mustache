import { PrismaClient } from "@prisma/client";

export class {{pascalCase}}Service extends PrismaClient {
    constructor() {
        super();
    }

    async connect() {
        console.log("Connecting to {{kebabCase}} service...");
        await this.$connect();
    }
}
